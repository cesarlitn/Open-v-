# Frontend environment (Vite). Copy to frontend/.env to override.
# Only VITE_-prefixed vars are exposed to the browser.

# CLIP model used for in-browser visual similarity search.
VITE_CLIP_MODEL=Xenova/clip-vit-base-patch16
